/* exported getValue */

// Define a function named getValue with two parameters: object, key
// This function should do:
//   gets the value in object found at the key property and returns it

function getValue(object, key) {
  return object[key];
}
