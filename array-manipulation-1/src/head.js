/* exported head */

// Define a function named head with one parameters: array.
// This function should:
//   use a for loop to loop through the values of array:
//      assign the value of array at i to a variable firstElement
//   return firstElement

function head(array) {
  var firstElement = 0;
  firstElement = array[0];
  return firstElement;

}

// Define a function named head with one parameters: array.
// This function should:
//   assigns number 0 to the variable firstElement
//   assigns the value of array at index 0 to the variable firstElement
//   returns the variable firstElement
