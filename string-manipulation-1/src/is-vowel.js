/* exported isVowel */

function isVowel(char) {
  if (char === 'a' || char === 'i' || char === 'u' || char === 'e' || char === 'o' || char === 'A' || char === 'I' || char === 'U' || char === 'E' || char === 'O') {
    return true;
  } else {
    return false;
  }
}
